const { ContactRepository } = require('./repository.js')
const { UserRepository } = require('./userRepository.js')

console.log('4====', UserRepository);

module.exports = {
    ContactRepository,
    UserRepository
}

